import 'package:flutter/material.dart';



class onbroaditem extends StatelessWidget {
  const onbroaditem({super.key, required this.pageController, required this.pageContent});

 final PageController pageController;
 final List pageContent;

  @override

  Widget build(BuildContext context) {
    return const Column(
      children: [ 

      ],
    );
  }
}
